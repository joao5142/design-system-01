{
  "name": "@design-system-01",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "packageManager": "npm@9.6.0",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "eslint-config-prettier": "^9.1.0",
    "turbo": "^2.1.3"
  }
}
